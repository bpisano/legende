---
type: llm
---

Pass if every French "?" or "!" in the proposed UI strings is preceded by a non-breaking space — shown as an escape such as  , \u{202F},  , or explicitly stated to be a non-breaking space. Fail if the strings use a plain typed space before "?" or "!" with no mention of non-breaking.
