---
description: Building a new screen without mentioning wording still triggers the skill and yields minimal copy.
tags: [auto-trigger, swiftui, en]
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill, Write]
---

Write a SwiftUI view `ProjectsListView` that shows a list of projects, an empty state when there are none, a button to create a project, and feedback after a project is successfully duplicated from a swipe action.

Write the final result to `out/ProjectsListView.swift`. All changes are pre-approved — apply them without asking.
