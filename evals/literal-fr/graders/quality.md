---
type: llm
focus:
  source: file
  path: out/fr.json
---

The corrected JSON:
- sounds like native French a user would say aloud (e.g. "Passer à Pro", "C'est bon", "C'est parti", "Rester connecté", "Aucun résultat"),
- uses "tu" consistently, never "vous",
- uses "Réglages" for settings.title (Apple platform vocabulary),
- the generic error says what to do (e.g. retry) instead of "Oups",
- uses French typography: a non-breaking space before "!" and "?" if they remain, sentence case.
